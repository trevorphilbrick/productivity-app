const shouldShowWeather = false;
const shouldShowEvents = false;
const defaultTheme = "system";
const shouldShowDashboardLinks = false;

export const flags = {
  shouldShowWeather,
  shouldShowEvents,
  defaultTheme,
  shouldShowDashboardLinks,
};
