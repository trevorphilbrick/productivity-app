const shouldShowWeather = false;
const shouldShowEvents = false;
const defaultTheme = "light"; // "system" | "light" | "dark"

export const flags = {
  shouldShowWeather,
  shouldShowEvents,
  defaultTheme,
};
